[[section-deployment-view]]


== Verteilungssicht

["plantuml", "dependencies", "svg"]
------------------------------------------------
include::../images/dependencies.puml[]
------------------------------------------------


=== JAR-Datei

Die einzelnen Domains von jFachwert werden als JAR-Dateien unter der GroupID "de.jfachwert" ins http://search.maven.org[zentrale Maven-Repository] hochgeladen.
Dort können sie über foldenden Maven-Kooridnaten als Abhängigkeit in ein Projekt hinzugefügt werden:

  <groupId>de.jfachwert</groupId>
  <artifactId>bank</artifactId>

Analog zu "bank" können auch andere Domain-Bereiche verwendet werden.


=== Abhängigkeiten

Externe Abhängigkeiten zu andere Bibliotheken beschränkt sich auf `commons-lang3` und der Kotlin-Standard-Bibliothek.
Bei Verwendung von Maven oder Gradle werden diese Abhängigkeiten automatisch hinzugefügt.
